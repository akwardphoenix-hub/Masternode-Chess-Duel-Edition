{
  "name": "masternode-chess-duel-edition",
  "version": "1.0.0",
  "description": "The coolest Chess game ever created",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:e2e": "npm run build && playwright test",
    "test:ci": "npm run build && playwright test --headed=false"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "vite": "^5.0.0"
  }
}
